# add AFL's binaries to the path.
# default path is set in the docker image
export PATH="/workspace/AFLplusplus-mod:$DEFAULT_PATH"
# add a variable defining where the libraries are
export AFL_PATH="/workspace/AFLplusplus-mod"
